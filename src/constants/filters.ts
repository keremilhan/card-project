import type { SubscriptionType, InstalledOption } from '../types';

export const subscriptionTypes: SubscriptionType[] = ['All', 'Paid', 'Free'];

export const installedOptions: InstalledOption[] = ['All', 'Installed', 'Not Installed'];

export const categories = ['All', 'Category 1', 'Category 2', 'Category 3'];
